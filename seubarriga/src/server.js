// const express = require( 'express' );

// const app = express();

// app.get( '/', ( req, res ) => {

//     res.status( 200 ).send();

// } );
const app = require( './app' );

app.listen( 3001 );
